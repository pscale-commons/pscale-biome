node_modules/
dist/
build/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.next
.turbo

# Claude Code worktrees (per-checkout, never share)
.claude/

# mobius-3 agent run output (runs happen in isolated working copies, never in source)
src/agent/filmstrip/
