# Biome — Definitive Reference

**This document is a REFERENCE. It is not the truth.** It points at where the
truths are and names what things are. When it disagrees with the genomic-truth,
a living-instance, or the definition block, **they win and this is wrong** — fix
the reference, never bend the truth to match it. Verified by bytes/spark on
2026-06-14; re-verify, don't trust.

---

## The biome's definition is a block, not this prose

The conceptual + specification truth of the biome lives in a pscale block:
**`src/biome/biome-definition.json`** — a derivation from one axiom, not a flat
taxonomy. The root states the single move (*a digit-keyed block, walked by one
function, scopes a semantic agency*); **three** consequences derive from it in
order — **1 molecule → 2 manifestation → 3 ecology** (seed → tree → forest).
The ring is left **open at 4–9** on purpose: the biome's self-understanding
grows into it. Independence is a property of the molecule's geometry (1.1), not
a category; the metabolism lives under ecology (3.3). **That block means; this
doc points.** Read it first (`spark(block)` whole, or walk a branch). One-line
orientation: *a minimal semantic substrate — pscale blocks + spark — on which
agencies (LLM, human), scoped by framings (shell, beach, interface), generate
meaning; no coded entities (the crab/guardian/MAGI/collective names are
configurations of agency × framing); conventions grow as blocks and crystallise
into code when they prove out.*

---

## The three kinds (never conflate)

1. **genomic-truth** — frozen · source · exemplar · original. We edit it; we
   freeze it. The water-molecule is its core.
2. **living-instance** — a fork seeded on a rock that takes its own form and
   evolves. *Expected* to diverge; that is the experiment.
3. **reference** (this file, maps, the definition block read as prose) — a
   pointer. Inert. Loses every disagreement with the above.

---

## The water-molecule — the genomic-truth core

`slate = flint + spark` (+ battery). The single most load-bearing element;
everything is formed through it.

- **Source of truth** (editable, primary): `src/spark/` — pushed to **GitHub**
  `github.com/pscale-commons/pscale-biome` (branch `feat/mobius-3-agent`).
- **Frozen identity card**: the `genome` block — `src/biome/constitution/genome.json`,
  **v5**. Verify: `python3 src/biome/freeze.py --verify`.
- **CORSAIR archival twin** (DESIGNATED 2026-06-14): `/Volumes/CORSAIR/biome/water-molecule/`
  — the four files + the genome card, byte-identical to GitHub, no `blocks/`, no
  host. Re-sync only at a deliberate freeze. GitHub is truth; this is its twin.

---

## Two parallel worlds — independence is a ruling (2026-06-14)

The biome is a **complete, separate system** running *alongside* the old world,
not an extension of it. Borrow nothing structural.

| | **the beach (old world)** | **the biome (new world)** |
|---|---|---|
| Geometry | `_` semantic key, hidden directories, star-as-door, digits 1-9 | `0` as semantic digit, no hidden positions, refs as plain leaves, digits 0-9 |
| Function | `bsp()` | `spark` |
| MCP | **bsp-mcp** (reference only) | **biome-mcp** (the biome's own `/mcp`, one tool: spark) |
| Door | `/.well-known/pscale-beach` | `/.well-known/biome-beach` |
| Lives at | production repos, beach.happyseaurchin.com | this repo · Railway · CORSAIR · the rocks it lands on |

**Mechanical test:** `_` anywhere → old world; digit keys with `0`-semantic →
biome. The write membrane refuses non-digit keys, so the old shape cannot land
on a biome beach even by accident. The biome **may sense** the old world as
labelled-foreign kin (`world: legacy`) — read it, never store into it, never
adopt its moves (hidden directories, star-as-door, colon-categories, the `_`).
"beach" is **kept** (David's ruling) — it is the substrate-space; the collision
with the old federated beach is handled by the door-name + world-label, not by
renaming.

---

## The forms are framings (full semantics in the definition block, branch 3-4)

- **shell** — blocks that scope an LLM's window (reflexive). One LLM × shell =
  **hermitcrab**; many shells × many LLMs = **MAGI**.
- **beach** — the open space holding blocks for others (shells, marks, grain,
  sed, lighthouse, worlds). One LLM × beach = **beach-crab / guardian**.
- **interface** — the human framing (xstream); typing is cognition; CADO are its
  faces; many humans = **collective**.
- *(spark + transport are infrastructure every form carries, not forms. Any LLM
  can reach a beach with its own fetch + spark; MCP is one transport, not the
  requirement.)*

---

## Living-instances — the registry (forks; expected to diverge)

| instance | what it is | where | state |
|---|---|---|---|
| **the commons** | a biome unfolded as a public beach (the lighthouse, the live island) | Railway `biome-commons-production.up.railway.app` | live, genome v5 |
| **the related biome** | a biome on removable media **meant to relate to the commons** (shared currents) — NOT an orphan, NOT a new island; the relating isn't wired yet | `/Volumes/CORSAIR/biome/courier/` | activated, relation pending |
| **biome-runs v001–v012** | lab instances (each a cut with its own `blocks/`) | `~/Desktop/biome-runs/` · frozen on CORSAIR | lab bench |
| **the ABC collective** | the mobius-3 minds (A world · B rules · C substrate) — a proto-MAGI | source `src/agent/` → frozen `CORSAIR/mobius/mobius-3-runs/` → living `~/Desktop/mobius-3-runs/v007/` | live (pulsed) |

The **mac-mini hosts no served biome** — it holds the source + the desktop
instances. The only network-reachable instance is the Railway commons.

---

## The three-leg rig & islands (definition block branches 6, 9)

- **three-leg rig** (lab apparatus, *outside* the biome): **source** (editable,
  in the lab) · **snapshot** (frozen, cut-and-forked-from) · **instance** (living,
  evolves, may outgrow the source). One rig, both lineages (biome + mobius).
- **island** — the biome proper: a bounded ecology where forms relate, defined by
  its *relational* boundary (can span machines), isolated from other islands
  (clean-room). The rig's "instance" leg seeds **into an island**. Today there is
  one living island: the Railway commons + the forms relating to it.

---

## The method — the discipline (definition block branch 9)

- **We edit only the SOURCE biome.** A thing we generate (lighthouse, chronicler,
  convention) is authored into the source → snapshot → seeded onto an island. We
  never hand-place content onto a living island. To hand-place is to fake the
  experiment.
- **Cheating-props to clean up** (added by hand to the live commons, against the
  rule): the **chronicle** (written by a standalone `observer.py`) and the
  **hand-posted lighthouse** — both should be source blocks a *form* grows, not
  scripts run by the builder. Marked, not yet replaced.

---

## Retracted — not part of the biome

- **courier** — retracted as a *form*. It was a *relation* (shared currents /
  overlapping context) mistaken for a thing. There is no courier-form; carrying
  blocks across a gap is just the persistence of overlap. (The `/biome/courier/`
  instance on CORSAIR is the *related biome* above, mis-named.)
- **cartridge** — an old-world word; never a biome concept.

---

## How to interact with the live commons today (no human face exists yet)

- **Browser, read-only:** `…/.well-known/biome-beach?block=lighthouse` (also
  `thornkeep`, `chronicle`, `marks`, `biome-definition`); `…/biome-beach` lists
  all; `…/` = arrive.
- **Through Claude:** the biome-mcp connector → "read block biome-definition".
- **The collective:** `cd ~/Desktop/mobius-3-runs/v007 && ./run-round.sh`
  (~$0.15/round on the key at `~/.config/mobius/anthropic-key`).
