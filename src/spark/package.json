{
  "name": "spark",
  "version": "0.1.0",
  "type": "module",
  "description": "spark — the coded kernel for pscale blocks (TypeScript). Run tezt with: node --experimental-strip-types tezt/tezt_spark.ts"
}
