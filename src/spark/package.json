{
  "name": "spark",
  "version": "0.1.0",
  "type": "module",
  "description": "spark — the coded kernel for pscale blocks (TypeScript). Run the battery with: node --experimental-strip-types test-battery/spark-battery.ts"
}
